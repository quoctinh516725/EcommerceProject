BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[master_orders] ADD [total_amount] DECIMAL(12,2) NOT NULL CONSTRAINT [master_orders_total_amount_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
