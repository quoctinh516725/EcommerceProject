BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[vouchers] ADD [per_user_limit] INT NOT NULL CONSTRAINT [vouchers_per_user_limit_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
